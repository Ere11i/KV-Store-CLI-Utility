# Основные зависимости для kv-store CLI утилиты

# Для тестирования
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# Для работы с JSON (встроенный в Python, но указан для ясности)
# json - встроенный модуль Python

# Для работы с потоками (встроенный в Python)
# threading - встроенный модуль Python

# Для работы с файлами и путями (встроенный в Python)
# pathlib - встроенный модуль Python

# Для работы с временными файлами в тестах (встроенный в Python)
# tempfile - встроенный модуль Python

# Для работы с аргументами командной строки (встроенный в Python)
# argparse - встроенный модуль Python

# Для работы с временем (встроенный в Python)
# datetime - встроенный модуль Python

# Для работы с коллекциями (встроенный в Python)
# collections - встроенный модуль Python

# Для работы с concurrent futures в тестах (встроенный в Python)
# concurrent.futures - встроенный модуль Python

# Для работы с random в тестах (встроенный в Python)
# random - встроенный модуль Python

# Дополнительные инструменты для разработки (опционально)
black>=22.0.0          # Форматирование кода
flake8>=5.0.0          # Линтинг
mypy>=1.0.0            # Проверка типов
isort>=5.10.0          # Сортировка импортов

# Для создания документации (опционально)
sphinx>=5.0.0          # Генерация документации
sphinx-rtd-theme>=1.0.0 # Тема для документации

# Для профилирования и оптимизации (опционально)
memory-profiler>=0.60.0 # Профилирование памяти
line-profiler>=4.0.0    # Профилирование строк кода
